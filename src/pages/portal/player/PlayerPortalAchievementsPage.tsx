import { Award, Trophy } from 'lucide-react';
import { BilingualText, bi } from '../../../components/bilingual/BilingualText';
import { usePlayerSession } from '../../../portals/player/PlayerSessionContext';
export function PlayerPortalAchievementsPage() { const { player } = usePlayerSession(); return <div className="player-page achievements-page"><header className="achievement-hero"><span><Trophy/></span><div><small><BilingualText value={bi('Your milestones','محطات إنجازك')}/></small><h1><BilingualText value={bi('Achievements','الإنجازات')}/></h1><p><BilingualText value={bi('Every verified milestone becomes part of your athlete story.','كل محطة موثقة تصبح جزءًا من قصتك الرياضية.')}/></p></div><strong>{player.achievements.length}</strong></header><section className="record-panel"><header><h2><BilingualText value={bi('Earned collection','مجموعة الإنجازات')}/></h2><span><BilingualText value={bi('Verified achievements','الإنجازات الموثقة')}/></span></header>{player.achievements.length ? <div className="achievement-grid">{player.achievements.map(item => <article key={item.en}><Award/><BilingualText value={item}/></article>)}</div> : <div className="player-empty"><span><Award/></span><h2><BilingualText value={bi('Your collection starts here','تبدأ مجموعتك هنا')}/></h2><p><BilingualText value={bi('Coach-verified achievements will appear as they are earned.','ستظهر الإنجازات التي يوثقها المدرب عند تحقيقها.')}/></p></div>}</section></div>; }
