import { CalendarClock } from 'lucide-react'; import { PageHeader } from '../../../components/admin/AdminUI'; import { BilingualText, bi } from '../../../components/bilingual/BilingualText'; export function ParentPortalSchedulePage() { return <div className="admin-page"><PageHeader eyebrow={bi('Parent Portal | Schedule', 'بوابة ولي الأمر | الجدول')} title={bi('Schedule', 'الجدول')} description={bi('Parent schedule.', 'جدول ولي الأمر.')} /><div className="admin-preview-card"><CalendarClock size={32} /><h3><BilingualText value={bi('Parent Schedule', 'جدول ولي الأمر')} /></h3></div></div>; }
