This folder is reserved for licensed local 3D/sport identity assets.

By policy these assets are not included in the repository by default.

If you add official assets, place them here using the filenames referenced in `src/design/sports3d/sports3d.registry.ts` (e.g. `football.png`).

LicenseStatus for each registry entry should be updated to 'ok' after review and inclusion.
